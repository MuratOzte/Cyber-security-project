import SearchBox from './SearchBox';

const Attack = () => {
    return (
        <div className="w-full flex justify-center">
            <SearchBox />
        </div>
    );
};

export default Attack;
