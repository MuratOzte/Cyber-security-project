import RegisterContainer from './RegisterContainer';

const Auth = () => {
    return (
        <div className="select-none w-full flex justify-evenly items-center -translate-y-16">
            <RegisterContainer />
        </div>
    );
};

export default Auth;
