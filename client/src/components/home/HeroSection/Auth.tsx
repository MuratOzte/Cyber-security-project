import RegisterContainer from './RegisterContainer';

const Auth = () => {
    return (
        <div className="select-none w-1/2 flex justify-evenly items-center -translate-y-16">
            <RegisterContainer />
        </div>
    );
};

export default Auth;
