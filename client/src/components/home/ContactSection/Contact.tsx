import Tooltip from '../../common/Tooltip';

const Contact = () => {
    return (
        <div className="w-full h-[100vh] flex justify-center items-center">
            <Tooltip text='Merhaba'>
                Selam
            </Tooltip>
        </div>
    );
};

export default Contact;
